The ``Square`` class defines a square.

>>> import importlib.machinery
>>> Square = importlib.machinery.SourceFileLoader(
...     "Square", "0-square.py"
... ).load_module().Square

Create an instance of Square:
>>> s = Square()
>>> isinstance(s, Square)
True